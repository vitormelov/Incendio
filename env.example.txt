# Variáveis de Ambiente - Frontend
# Copie estas variáveis para o Vercel (Settings > Environment Variables)
# OU crie um arquivo .env.local na raiz do projeto

# Evolution API Configuration
# URL da Evolution API (ex: https://sua-evolution-api.railway.app ou http://localhost:8080)
VITE_EVOLUTION_API_URL=http://localhost:8080

# API Key da Evolution API (deve ser igual ao AUTHENTICATION_API_KEY do docker-compose.yml)
VITE_EVOLUTION_API_KEY=INCENDO_FACIL123

# Nome da instância do WhatsApp criada na Evolution API
VITE_EVOLUTION_INSTANCE_NAME=incendio-bot

# ID do grupo do WhatsApp (formato: 5511999999999@g.us)
# Para obter: adicione o bot ao grupo e veja o ID no manager da Evolution API
VITE_WHATSAPP_GROUP_ID=

# Firebase já está configurado em src/firebase/config.ts
# Não precisa de variáveis de ambiente para Firebase

